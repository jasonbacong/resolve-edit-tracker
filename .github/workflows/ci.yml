name: CI

on:
  push:
    branches: [main]
  pull_request:

concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

jobs:
  build:
    runs-on: macos-15
    steps:
      - uses: actions/checkout@v5

      - name: Swift version
        run: swift --version

      - name: Build (debug)
        run: swift build

      - name: Assemble .app bundle
        run: ./make_app.sh

      - name: Sanity-check the bundle
        run: |
          test -x "dist/Editor Tracker.app/Contents/MacOS/EditorTracker"
          /usr/libexec/PlistBuddy -c "Print CFBundleIdentifier" "dist/Editor Tracker.app/Contents/Info.plist"
          codesign --verify --verbose "dist/Editor Tracker.app"
